name: install packages
hosta: all
become: yes
tasks: 
  - name: install package
    yum: 
      states: present
      name: {{item}}
    loop:
      - httpd
      - tree
      - vim
      - java  